# 📋 Що робить кожна команда — Premium Bot (20 команд)

### ℹ️ ІНФО (для всіх)
- **/ping** — показує затримку бота в мс. `🏓 Понг! 45ms`
- **/help** — меню з усіма командами по категоріях
- **/server** — інфо про сервер: власник, кількість людей, дата створення
- **/user [@юзер]** — інфо про юзера: ID, тег, коли зайшов на сервер. Без @ — показує тебе
- **/avatar [@юзер]** — велика аватарка 512px

### 🛡️ МОДЕРАЦІЯ (тільки для адмінів)
- **/clear [кількість 1-100]** — видаляє останні повідомлення. Потрібно `ManageMessages`
- **/kick [@юзер] [причина]** — виганяє з сервера (може зайти знову). `KickMembers`
- **/ban [@юзер] [причина]** — банить назавжди. `BanMembers`
- **/unban [ID]** — розбанює по ID (навіть якщо юзера нема на сервері)
- **/timeout [@юзер] [хвилини 1-10080]** — мутить (не може писати/говорити). `ModerateMembers`
- **/warn [@юзер] [причина]** — варн, лічильник в database.json (1/3, 2/3...)

### 💰 ЕКОНОМІКА (працює на кожному сервері окремо)
- **/balance [@юзер]** — баланс монет. Старт 1000 (config.json)
- **/daily** — щоденні 500 монет, раз в 24 год. Якщо раніше — пише `через X год`
- **/work** — заробити 100-300 рандомних монет
- **/pay [@юзер] [сума]** — переказати монети іншому

### ⭐ РІВНІ (XP за кожне повідомлення)
- **/rank [@юзер]** — показує рівень і XP `0/300`, `XP+15+рандом`
- **/leaderboard [тип: level/money]** — топ-10 по рівню або по монетах на сервері. Автоматично пише `🎉 досяг X рівня!`

### 🎲 ФАН
- **/8ball [питання]** — куля передбачень: Так/Ні/Можливо/100%...
- **/coinflip** — орел чи решка

### 🎫 ТІКЕТИ (тільки адмін)
- **/ticket-panel** — створює ембед з кнопкою `📩 Створити тікет`
  - Юзер натискає → бот створює приватний канал `ticket-нік` (видно тільки юзеру і адмінам)
  - В каналі кнопка `🔒 Закрити тікет` → видаляє канал через 3 сек

> Всі гроші/рівні/варни зберігаються в `database.json` окремо для кожного сервера (`guildId_userId`).
